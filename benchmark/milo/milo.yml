name: milo
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.12
  - conda-forge::r-base
  - conda-forge::r-systemfonts
  - conda-forge::libxml2
  - pip
  - pip:
      - pertpy[de]==0.10.0
      - rpy2==3.5.10
# Note that one needs to install miloR through BiocManager::install('miloR')
